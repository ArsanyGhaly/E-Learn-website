<!DOCTYPE html>
<html lang="en">
        <% include ../partials/header.ejs %>        
<body>
    
<% include ../partials/nav.ejs %>


    <!-- page title -->
<section class="page-title-section overlay" data-background="/images/backgrounds/page-title.jpg">
  <div class="container">
    <div class="row">
      <div class="col-md-8">
        <ul class="list-inline custom-breadcrumb">
          <li class="list-inline-item"><a class="h2 text-primary font-secondary"><%= title %></a></li>    
          <li class="list-inline-item"></li>  
        </ul>
        <p class="text-lighten"><br/><%= desc %></p>
      </div>
    </div>
  </div>
</section>

<!-- contact -->
<section class="section bg-gray">
  <div class="container">
    <div class="row">
      <div class="col-lg-12">
        <h2 class="section-title">Add Quiz</h2>
      </div>
    </div>
    <div class="row">
      <div class="col-lg-7 mb-4 mb-lg-0">
        <% if (error && error.length > 0) { %>
    <div class="alert alert-danger container text-center text-capitalize">
        <%= error %>
    </div>
    <% } %>
    <% if (success && success.length > 0) { %>
    <div class="alert alert-success container text-center text-capitalize">
        <%= success %>
    </div>
    <% } %>



<% if(typeof errors !== "undefined"){ %>
      <% Object.keys(errors).forEach(function(error){ %> 
      <div class="alert alert-danger container text-center text-capitalize">
        <%= error.msg %>
      </div>
      <% }); %>
   <% }else{console.log("mdejijeij");} %>
</div>
</div>
</div>
</section>       

<form class="text-center border border-light p-5" method='post' action='/teachers/addclass' enctype="multipart/form-data">
<input type="text" class="form-control form-control-sm" id="name" name="title" placeholder="Write the quiz title">

<input type="text" class="form-control form-control-sm" id="name" name="total points" placeholder="Total points for quiz">

 

<!-- notice -->
<section class="section">
  <div class="container">
    <div class="row">
      <div class="col-12">
        <ul class="list-unstyled">
          <li class="d-md-table mb-4 w-100 border-bottom hover-shadow">
          <h4>Question <br/></h4>
            <input type="text" class="form-control form-control-sm" id="name" name="question" placeholder="Write the question">
          <h4>choocies <br/></h4>
          <input type="text" class="form-control mb-3" id="name" name="a" placeholder="a.">
         
         <input type="text" class="form-control mb-3" id="name" name="b" placeholder="b.">

          <input type="text" class="form-control mb-3" id="name" name="c" placeholder="c.">

          <input type="text" class="form-control mb-3" id="name" name="d" placeholder="d.">
          <h4>Right Answer <br/></h4>
          <input type="text" class="form-control mb-3" id="name" name="right" placeholder="The Right answer">
        
          <h4>Grade<br/></h4>
          <input type="text" class="form-control mb-3" id="name" name="grade" placeholder="Grade">
          <button type="submit" value="send" class="btn btn-primary mt-0">Add Question</button>
          
          <div class="row">
      <div class="col-lg-7 mb-4 mb-lg-0">
        <% if (error && error.length > 0) { %>
    <div class="alert alert-danger container text-center text-capitalize">
        <%= error %>
    </div>
    <% } %>
    <% if (success && success.length > 0) { %>
    <div class="alert alert-success container text-center text-capitalize">
        <%= success %>
    </div>
    <% } %>



<% if(typeof errors !== "undefined"){ %>
      <% Object.keys(errors).forEach(function(error){ %> 
      <div class="alert alert-danger container text-center text-capitalize">
        <%= error.msg %>
      </div>
      <% }); %>
   <% }else{console.log("mdejijeij");} %>
</div>
</div>

          
          </li>
        
        </ul>
      </div>
    </div>
  </div>
</section>

          <button type="submit" value="send" class="btn btn-primary mt-0">Add quiz</button>
      
        </form>
      </div>


    <% include ../partials/footer.ejs %>    
    <% include ../partials/foot.ejs %>  
    </body>
    
</html>
